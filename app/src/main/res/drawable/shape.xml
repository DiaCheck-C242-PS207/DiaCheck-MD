
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <!-- Warna latar belakang tombol -->
            <solid android:color="@color/orange" />

            <!-- Warna garis luar -->
            <stroke
                android:width="2dp"
                android:color="#FE5E49" />

            <!-- Radius sudut -->
            <corners android:radius="8dp" />
        </shape>
    </item>
</selector>
